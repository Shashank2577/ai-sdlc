# Test files (test_*.py / test-*.sh) that no workflow executes, on purpose.
#
# scripts/check-test-wiring.sh treats every other such file as a bug: a
# test nobody runs is a test that can fail for weeks with every check
# green (#69). An entry here is the deliberate exception, not a shortcut —
# it must name a real reason, or the check rejects the entry itself.
#
# Format: one entry per line, "<path from repo root><TAB><reason>".
# Blank lines and lines starting with # are ignored.

# #123 (P3-4): adapters/tracker/ is a new, standalone seam with nothing
# wired to it yet — see the PR. It needs a new workflow file to run its
# tests in CI, but this session's push credential lacks the `workflow`
# OAuth scope (GitHub rejects any push that touches .github/workflows/**
# without it), so the workflow could not be added here. A draft is in the
# PR body; a maintainer should add it and remove these two lines.
adapters/tracker/tests/test_github.py	blocked on `workflow` scope — see PR #123
adapters/tracker/tests/test_jira.py	blocked on `workflow` scope — see PR #123
